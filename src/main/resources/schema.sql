CREATE TABLE IF NOT EXISTS users
(
    user_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    login VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    name VARCHAR(255),
    birthday DATE
);

CREATE TABLE IF NOT EXISTS genres
(
    genre_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS mpa (
    mpa_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(5) NOT NULL
);

CREATE TABLE IF NOT EXISTS films
(
    film_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(128) NOT NULL,
    description VARCHAR(200),
    release_date DATE,
    duration INT,
    mpa_id BIGINT REFERENCES mpa(mpa_id)
);

CREATE TABLE IF NOT EXISTS films_genres
(
    film_id BIGINT NOT NULL REFERENCES films(film_id),
    genre_id BIGINT NOT NULL REFERENCES genres(genre_id)
);

CREATE TABLE IF NOT EXISTS friends
(
    user_id BIGINT NOT NULL REFERENCES users(user_id),
    friend_id BIGINT NOT NULL REFERENCES users(user_id)
);

CREATE TABLE IF NOT EXISTS likes
(
    film_id BIGINT NOT NULL REFERENCES films(film_id),
    user_id BIGINT NOT NULL REFERENCES users(user_id)
);

ALTER TABLE FRIENDS
DROP CONSTRAINT CONSTRAINT_70;

ALTER TABLE FRIENDS
ADD CONSTRAINT FK_FRIENDS_USERS FOREIGN KEY (USER_ID) REFERENCES USERS (USER_ID) ON DELETE CASCADE;

ALTER TABLE LIKES
DROP CONSTRAINT CONSTRAINT_45;


ALTER TABLE likes
ADD CONSTRAINT FK_likes_films FOREIGN KEY (film_id) REFERENCES films (film_id) ON DELETE CASCADE;

ALTER TABLE friends
ADD CONSTRAINT FK_friends_users_user_id FOREIGN KEY (user_id) REFERENCES users (user_id) ON DELETE CASCADE;
ALTER TABLE friends
ADD CONSTRAINT FK_friends_users_friend_id FOREIGN KEY (friend_id) REFERENCES users (user_id) ON DELETE CASCADE;

ALTER TABLE films_genres
ADD CONSTRAINT FK_films_genres_films FOREIGN KEY (film_id) REFERENCES films (film_id) ON DELETE CASCADE;
ALTER TABLE films_genres
ADD CONSTRAINT FK_films_genres_genres FOREIGN KEY (genre_id) REFERENCES genres (genre_id) ON DELETE CASCADE;